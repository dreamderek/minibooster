# miniboot 需求規格

## 1. 專案目的
建立一個可在手機上操作的數學多選題練習頁，提供：

- 題目閱讀
- 多選作答
- 送出評分
- 檢討與回饋

此頁面採單頁式介面，透過左右滑動在「作答頁」與「檢討頁」之間切換。

## 2. 頁面結構

### 2.1 頂部表頭
- 顯示品牌名稱 `MetaMath`
- 提供兩個切換按鈕
  - 作答頁
  - 檢討頁

### 2.2 作答頁
作答頁包含：

- 題目區
- 選項區
- 送出按鈕

### 2.3 檢討頁
檢討頁僅用來呈現結果，不再進行作答計分。  
檢討頁包含：

- 本次作答結果摘要
- 能力分析條
- 選項作答明細
- 建議文字

## 3. 功能需求

### 3.1 題庫載入
- 頁面啟動時必須自 `data.json` 載入題庫資料
- 載入失敗時要顯示錯誤訊息
- 載入成功後才可開始作答

### 3.2 題目顯示
- 題目支援一般文字與數學排版
- 題目上方需顯示 meta 標籤
- 題目下方需顯示提示文字

### 3.3 選項作答
- 每個選項使用 checkbox
- 允許多選
- 每個選項須顯示：
  - 選項代號
  - 選項文字
  - 選項補充說明

### 3.4 送出與檢查
- 使用者未勾選任何答案時，不可直接進入結果檢討
- 按下送出後，系統要依正確答案比對作答狀態
- 送出後自動切換到檢討頁

### 3.5 評分規則
- 正確選中 +1
- 錯誤選中 -1
- 漏選正確答案 -1
- 最終分數不可小於 0

### 3.6 檢討結果
- 顯示總分
- 顯示能力等級
- 顯示摘要說明
- 顯示建議文字
- 顯示三個能力指標條
- 逐項列出每個選項的判定結果

## 4. 檢討頁判定規則

檢討頁需顯示每個選項的對錯與狀態，狀態定義如下：

- `正確`：有選且為正確答案
- `錯誤`：有選但不是正確答案
- `漏選`：正確答案但未被選取
- `未作答`：尚未選取，且不屬於正確答案

另外，檢討頁的建議文字由 `data.json` 預先提供，應依作答結果顯示對應內容。

## 5. 視覺與互動需求

### 5.1 版面
- 以手機尺寸優先設計
- 內容需可在小螢幕上正常閱讀
- 作答頁與檢討頁都需可垂直捲動
- 左右滑動可切換頁面

### 5.2 框線與標題
- 題目區、選項區、檢討區皆需顯示有標題的框線元件
- 標題需浮在框線外側
- 標題與內容區底色需一致
- 同一套框線與標題樣式需共用

### 5.3 狀態回饋
- 未作答就送出時，題目區需有明顯提示效果
- 檢討頁需讓使用者能快速看出對錯與能力落點

## 6. 資料格式需求

`data.json` 需提供以下欄位：

### 6.1 基本欄位
- `title`：題目文字
- `prompt`：作答提示
- `meta`：題目標籤陣列
- `options`：選項陣列
- `grading`：評分設定

### 6.2 `options` 結構
每個選項需包含：

- `id`
- `text`
- `note`
- `correct`

### 6.3 `grading` 結構
評分設定需包含：

- `correctAnswers`
- `tiers`
- `optionFeedback`

### 6.4 `tiers` 結構
每個等級需包含：

- `minScore`
- `level`
- `summary`
- `advice`

### 6.5 `optionFeedback` 結構
每個選項需提供：

- `correct`
- `incorrect`

## 7. 自訂元件需求

### 7.1 `frame-panel`
框線與標題效果需封裝成獨立自訂元件。

元件需求：

- 以 `title` 屬性設定標題文字
- 元件內部自行繪製框線
- 標題需跨在框線外側
- 標題底色需與內容底色一致
- 元件需保留 `shake` 動畫能力

### 7.2 使用方式
- 題目區、選項區、檢討區皆應使用同一個元件
- 外層頁面只負責放入內容，不重複寫框線與標題邏輯

## 8. 驗收標準

完成後需符合以下條件：

- 頁面能正常載入
- `data.json` 能成功讀取
- 題目、選項、檢討頁內容正常顯示
- 送出後能切換至檢討頁
- 計分結果與正確答案一致
- 檢討頁的選項判定與規則一致
- 框線標題不被裁切
- 三個區塊的標題樣式一致
- `frame-panel.js` 可獨立維護

## 9. 備註

- 目前專案定位偏向單機版練習頁，不含後端儲存
- 如後續要支援多題切換、答題紀錄或成績保存，可在此規格上擴充
